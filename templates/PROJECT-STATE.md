# <项目名> · 项目现状(PROJECT-STATE)

> 此为 tracked 稳定正文；NotebookLM 使用 `state_snapshot.py` 生成的同名运行快照。
> 稳定段置前；HEAD、需求 hash、diff、Pending 索引及决策包仅在快照尾部生成。

## 当前迭代目标

- `<一项已批准目标>`

## 已验证代码事实

- `<事实 + 文件/symbol/命令证据>`

## 相关模块与 symbol

- `<path/symbol/CodeGraph query>`

## 最近完成与当前 diff

- 最近完成:`<已验证变更>`
- 当前 diff:`<有界摘要>`

## 验证状态

- `<测试/typecheck/lint/build + 退出码>`

## 当前失败信号与风险

- 失败信号:`<稳定错误签名或无>`
- 风险:`<已证风险；推测另标为未决>`

## 架构边界

- 目标/非目标:`<一句话>`
- 锁定决策:`<少改的决策+理由>`
- 基线依据:`<commit / CodeGraph status>`
- 模块与落点:`<局部事实>`
- 关键接口/直接路径:`<已验证 symbol>`

## 需求—代码—测试追踪

| Active REQ | 状态 | 代码证据 | 测试/质量证据 |
| --- | --- | --- | --- |
| `REQ-<...>` | implemented/gap | `<path/symbol>` | `<test/gate>` |

## Known failed approaches

- `_暂无已验证失败路径_`

## 下一项已批准工作

- `<一项；无则写无>`

## 本轮 delta

- 变更:`<paths/symbols>`
- 直接影响:`<impact/affected 或不可判>`
- 验证:`<命令 + exit + evidence>`
- 质量:`<计数/相对基线/待处理 ID>`

<!-- 以下运行时段由 state_snapshot.py 生成，不写回本文件：
repository_head / requirements_version / requirements_hash / generated_at /
current_git_diff / 非权威 Pending 索引 / 当前决策包
-->
